apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  generation: 2
  name: version2
  namespace: trivago
spec:
  ingressClassName: traefik
  rules:
  - host: version2.trivago.apps.com
    http:
      paths:
      - backend:
          service:
            name: version2
            port:
              number: 80
        path: /
        pathType: Prefix
